global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/producer-pool-recording.yml
  - /etc/prometheus/rules/producer-pool-alerts.yml

scrape_configs:
  - job_name: producer-pool
    static_configs:
      - targets: [producer-pool-demo:9404]
